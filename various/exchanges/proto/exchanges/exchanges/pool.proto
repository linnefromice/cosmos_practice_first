syntax = "proto3";
package exchanges.exchanges;

option go_package = "exchanges/x/exchanges/types";

message Pool {
  uint64 id = 1;
  string address = 2;
  string denom = 3; 
  bool isActive = 4; 
  uint64 normalDeposited = 5; 
  uint64 conlyDeposited = 6; 
  uint64 borrowed = 7; 
  
}
